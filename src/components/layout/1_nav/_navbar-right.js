/**
 * NAVBAR Right
 * INFO: Right side of the navbar contains other information content of this component is not clearly defined
 * 
 */

import Markdown from 'markdown'

export default ({classes})=>{
    return (
        <div style={classes}>
            Navbar right
        </div>
    )
}